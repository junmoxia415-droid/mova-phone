<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application>
        <!--
          Filtrado de llamadas con la API oficial (CallScreeningService).
          Sólo actúa si el usuario concede el rol de filtrado de llamadas.
        -->
        <service
            android:name=".MovaCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!--
          MOVA Phone como aplicación de teléfono del sistema.
          Con el rol de teléfono concedido, Android entrega aquí TODAS las llamadas y
          MOVA muestra su propia pantalla de llamada (sin abrir la del sistema).
        -->
        <service
            android:name=".MovaInCallService"
            android:exported="true"
            android:permission="android.permission.BIND_INCALL_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_RINGING"
                android:value="true" />
        </service>

        <receiver
            android:name=".CallStateReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
