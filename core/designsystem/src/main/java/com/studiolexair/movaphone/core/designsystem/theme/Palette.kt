package com.studiolexair.movaphone.core.designsystem.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de marca MOVA Phone (Studio Lexair).
 * Azul/cian tecnológico sobre fondo oscuro elegante; rojo reservado a emergencias;
 * verde reservado a estados correctos. Nada de colores decorativos fuera de esta tabla.
 */
object MovaPalette {
    // Fondos
    val Ink = Color(0xFF04070F)
    val Night = Color(0xFF0A0F1A)
    val SurfaceLow = Color(0xFF0E1524)
    val Surface = Color(0xFF111A2B)
    val SurfaceHigh = Color(0xFF17223A)
    val Border = Color(0xFF1E2A41)

    // Marca
    val Cyan = Color(0xFF22D3EE)
    val SkyBlue = Color(0xFF38BDF8)
    val Blue = Color(0xFF2563EB)
    val Indigo = Color(0xFF4F7CFF)

    // Estados
    val Success = Color(0xFF22C55E)
    val Warning = Color(0xFFF59E0B)
    val Danger = Color(0xFFEF4444)
    val DangerDeep = Color(0xFFB3161C)
    val Violet = Color(0xFF8B5CF6)

    // Texto
    val TextPrimary = Color(0xFFF8FAFC)
    val TextSecondary = Color(0xFFA6B4CC)
    val TextMuted = Color(0xFF6C7C99)

    // Superficies claras (tema claro)
    val LightBackground = Color(0xFFF4F7FC)
    val LightSurface = Color(0xFFFFFFFF)
    val LightBorder = Color(0xFFD8E1F0)
    val LightTextPrimary = Color(0xFF0B1220)
    val LightTextSecondary = Color(0xFF4A5A75)
}
